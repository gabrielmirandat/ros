#include "tr5_zrobot/trabalho.h"

int main(int argc, char** argv)
{
    ros::init(argc, argv, "trabalho5");
    ros::NodeHandle n;                  // node handle global
    Trabalho tr(n);
    tr.spinTrab5();
}

